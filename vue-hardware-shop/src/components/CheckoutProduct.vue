<template>
  <div class="checkoutProduct">
    <img class="checkoutProduct__image" :src="product.image" :alt="product.title" />
    <div class="checkoutProduct__info">
      <p class="checkoutProduct__title">{{ product.title }}</p>
      <p class="checkoutProduct__title">{{ product.description }}</p>
      <p class="checkoutProduct__price">
        <small>$</small>
        <strong>{{ product.price }}</strong>
      </p>
      <button @click="removeFromBasket">Remove from Basket</button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { IProduct } from '@/models/product';
import { useStore } from '@/store/index';

interface Props {
  product: IProduct;
}

const { product } = defineProps<Props>();
const store = useStore();

const removeFromBasket = () => {
  store.removeFromBasket(product.id);
};
</script>

<style scoped>
.checkoutProduct {
  max-width: 1000px;
  display: flex;
  flex-basis: 500px;
  flex-grow: 1;
  margin: 10px;
  padding: 20px;
  border: solid black;
  border-radius: 15px;
  background-color: rgba(255, 255, 255, 0.5);
  -webkit-box-shadow: 0px 0px 100px 5px rgba(0, 0, 0, 0.9);
  -moz-box-shadow: 0px 0px 100px 5px rgba(0, 0, 0, 0.9);
  box-shadow: 0px 0px 100px 5px rgba(0, 0, 0, 0.9);
}

.checkoutProduct__info {
  padding-left: 20px;
}

.checkoutProduct__info > button {
  cursor: pointer;
  background: #f0c14b;
  font-size: 12px;
  font-family: 'Carter One', cursive;
  color: #111;
  padding: 8px;
  margin-top: 10px;
  border-radius: 20px;
  border-color: #f0c14b;
  transition: all ease-in 200ms;
}

.checkoutProduct__info > button:hover {
  font-size: 10px;
  -webkit-box-shadow: 0px 0px 40px 10px rgba(240, 190, 70, 0.5);
  -moz-box-shadow: 0px 0px 40px 10px rgba(240, 190, 70, 0.5);
  box-shadow: 0px 0px 40px 10px rgba(240, 190, 70, 0.5);
}

.checkoutProduct__image {
  object-fit: contain;
  width: 180px;
  height: 180px;
}

.checkoutProduct__rating {
  display: flex;
}

.checkoutProduct__title {
  font-size: 17px;
  font-weight: 800;
}

@media only screen and (max-width: 500px) {
  .checkoutProduct {
    margin: 5px;
    padding: 10px;
  }

  .checkoutProduct__image {
    width: 100px;
    height: 100px;
  }

  .checkoutProduct__title {
    font-size: 12px;
    font-weight: 400;
  }
}
</style>
